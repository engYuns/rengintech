# Database Configuration (REQUIRED for production)
# For Railway/Render/Heroku: This is auto-set by the platform
# For local PostgreSQL: postgresql://username:password@localhost:5432/database_name
# Leave empty to use file storage (data/storage.json)
DATABASE_URL=postgresql://username:password@localhost:5432/rengintech

# Session Secret (REQUIRED for production)
# Generate a random 32+ character string
# Mac/Linux: openssl rand -base64 32
# Or use: https://www.random.org/passwords/
SESSION_SECRET=change-this-to-a-random-32-character-string

# Server Configuration
# Most platforms (Railway, Render, Heroku) set PORT automatically
PORT=5000

# Environment
NODE_ENV=production
